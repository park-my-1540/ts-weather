{
  "source": {
    "include": ["src"],  // 소스 파일 경로
    "includePattern": ".+\\.(ts|tsx)$",  // TypeScript 파일 포함
    "excludePattern": "(node_modules|docs)"
  },
  "opts": {
    "destination": "./docs",  // 생성된 문서가 저장될 위치
    "recurse": true
  },
  "plugins": [
    "node_modules/jsdoc-typescript"  // TypeScript를 지원하기 위한 플러그인
  ]
}
